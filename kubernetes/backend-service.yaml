apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    - port: 7860
      targetPort: 7860
      protocol: TCP
  type: ClusterIP
